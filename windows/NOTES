How it will be done
mkwindows (UNDER CONSTRUCTION) will contain a cygwin/bash script for compiling everything
(apart from example script). 

To compile sources to

gcc mk/mkEverything.c -o mk/mkEverything
make preparehtk
windows/mkwindows

Then we need a way of producing a package (to sort out).
