/* default_options() returns a string representing the default
   options to be compiled into UniForM.  The format of the
   string is defined in WBFiles.hs.  The reason for
   putting the default options in a C and not a Haskell file
   is so that the default options can be changed easily without
   having to recompile everything. */
#include "default_options.h"

const char *default_options ="@DEFAULTOPTIONS@";
