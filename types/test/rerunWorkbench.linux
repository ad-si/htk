#!/usr/local/bin/bash
# Test script for running the workbench once the server is already going.
export UNISERVER=${UNISERVER:-titania.informatik.uni-bremen.de}
export UNIEDITOR="xterm -e ne %F"
gmake killWorkbench
gmake runWorkbench