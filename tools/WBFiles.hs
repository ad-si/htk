{- This module is supposed to provide simple implementions of
   the functionality used by the tools in the original WBTools in UniForM,
   without needing to use the OMS.  If possible.
   -}
module WBFiles() where
